{
  "name": "name",
  "private": true,
  "scripts": {
    "render-build": "curl https://install.meteor.com/ | sh && export PATH=$HOME/.meteor:$PATH && meteor npm install && meteor build --directory ./.output",
    "start": "cd ./.output/bundle/programs/server && npm install && cd ../../.. && PORT=$PORT ROOT_URL=$ROOT_URL MONGO_URL=$MONGO_URL node main.js"
  },
  "engines": {
    "node": "12.x"
  },
  "dependencies": {
    "@babel/runtime": "^7.10.2",
    "@blueprintjs/core": "^3.35.0",
    "@blueprintjs/icons": "^3.22.0",
    "bcrypt": "^4.0.1",
    "meteor-node-stubs": "0.4.1",
    "minimist": ">=0.2.1",
    "react": "16.5.2",
    "react-device-detect": "^1.14.0",
    "react-dom": "16.5.2",
    "react-router-dom": "4.3.1",
    "simpl-schema": "1.5.5"
  },
  "meteor": {
    "mainModule": {
      "client": "client/main.js",
      "server": "server/main.js"
    }
  }
}
